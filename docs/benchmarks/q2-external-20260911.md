# Q2 cached external comparison — 2026-09-11

Historical timing disposition: generation equivalence passed, but CPU preparation overlapped measurement, so timing controls are invalid and performance qualification is inconclusive. The original automated report does not override this limitation.

**Generation comparability passed; timing qualification is inconclusive. PR17 remains draft and Q2 remains open.** The frozen reporter validates all eight rows and reports a pass, but a subsequent chronology review found that the separate quality CPU probe overlapped a measured case on the same CPU set. Preserve the original report; its automated checks do not detect that external CPU activity. No speedup is qualified and no third attempt is run under the declared budget.

## Actual comparison and scope

Accepted PR14 compact native FP32 Triton tile32 is compared with the locally inspected official eager cached implementation at `ByteDance/Ouro-1.4B@574fa66cb8bf5abdc979642d01cf2b79b16bfab1`. W1 uses the same128 prompt IDs,64 actual greedy outputs, exactly four loops and ignored EOS. No forced continuation, adaptive policy, quality benchmark or serving claim is involved.

Both feasibility histories and every later history match all64 IDs at depth4. Each native row has380 scheduler steps. Each official row has one128-token prefill and63 one-token advances; its96 distinct depth/layer KV slots end at191 positions. The last prediction is not forwarded. All eight request/cache resets and final worker cleanup pass.

Native and official share immutable FP32 weight storage; the native6GiB pool remains resident in both implementations. Official dynamic KV adds300,417,024 bytes at final length. Official eager attention, cache concatenation and wrapper checks are included as implementation costs. Native final request release occurs inside the final engine step; official cache inspection/reset follows delivery. Loading, warmups and detailed post-delivery cache inspection are separate from delivered-token timing.

## Raw timing observations — unqualified

| Pair | Native tokens/s | Official tokens/s | Native / official |
| --- | ---: | ---: | ---: |
| pair-1 | 20.459672 | 17.583242 | 1.163589 |
| pair-2 | 20.340335 | 18.203034 | 1.117415 |

These are retained observations, not an accepted controlled speed comparison. Although their two-run ranges do not overlap, the quality CPU probe finalized its plan at01:53:30.755204UTC and log at01:53:30.781204UTC, within N2’s measured-case interval (started-file timestamp01:53:28.240189UTC, result-file timestamp01:53:31.390208UTC). Both commands used CPU0–7 and memory node0. The probe’s exact process-start time and causal effect on throughput were not measured. Neither removing an outlier nor choosing the other pair repairs this missing control.

Full quality CPU tests ended at01:50:18.855630UTC before this experiment; they did not overlap. The later quality **preparation probe** did. Its activity is recorded separately in `artifacts/q2-external-manual-timing-disposition.json`, with exact file hashes, timestamps, affinity and the executed command. The automated report remains unchanged; the manual timing decision is `inconclusive`.

## Execution, preparation and cleanup

The corrected experiment completed all8 rows in52.049211199 seconds, below the3600-second global and600-second case caps. There were two excluded feasibility runs, two warmups and four measured rows in N1–O1–O2–N2 order, one worker and one model load. Model loading took4.067065 seconds; complete model/adapter preparation took11.290688 seconds. Queueing and source verification precede those preparation figures. No profiler or tensor dump was taken.

Physical GPU2 was NVIDIA L20X, UUID `GPU-cbf66259-f4ab-0ede-1811-82037dde5924`, under a scheduler-owned reservation on the recorded host. CPU0–7, memory node0, OMP/intra-op/inter-op1, seed0, and disabled TF32/reduced-precision reductions were fixed for both implementations across all eight rows. The prepared Q1 environment and checkpoint were reused. Final task-owned allocated/reserved bytes were0/0; the independent post-run scheduler/device check showed AVAILABLE and0MiB.

An earlier reserved preflight stopped after11.761314598 seconds before any model load or case: NVIDIA’s prefixed UUID and PyTorch’s bare UUID were compared as different strings. It ran zero feasibility/warmup/measured cases, released task-owned memory to0/0, and its original reporter records invalid/inconclusive evidence. The device was subsequently reassigned, so that earlier post-check is not represented as globally idle. Its raw directory and original report are preserved.

The correction accepts only the two exact UUID representations and still rejects a different or malformed identity. It changes no native production, cached model adapter or generation/timing driver. One corrected attempt was explicitly declared before execution. No additional attempt is made after the later CPU-overlap finding.

## Source and validation

- Corrected execution: `3c99076eb305c5569a703cb169fc732f1c1a2696`, clean136-file snapshot; source code was committed before the resolved contract and run.
- Corrected canonical plan: `a80afaaaa707021eb84a79be02f54dc078574a5915a21243ae4ad5678eda84e8`; exact plan-file SHA256 `0de3120382e23fc0956265e539b108a02117c2e12b268daad27d6237a566a44a`.
- Original automated report: `6fbece89e83e4726b9e3152d9898ddc4ce5bfee2cdc5b700c7e32fd38ca1a81d`; decision passed, complete evidence,8 valid ACK rows/four measured rows, no recorded errors/missing/failures.
- Manual timing disposition: `8c949f6724cb1708305ae4ccdf0eff746464ff0380e9d60061afd8f648f84e64`; no accepted speed conclusion.
- CPU checks:761 passed/16 skipped on the initial implementation;46 focused tests after its report-only correction;83 focused tests after UUID handling was corrected. Ruff/format pass. The later edits to a test decorator were formatting only. No second full-suite run or unavailable memory-checker execution is claimed.
- Tiny CPU integration validated actual128→64 native/official generation, cache count and lengths with GPU bookkeeping stubbed; it is separate from the real checkpoint device evidence.
- Pre-run contracts: [original](https://github.com/hsliuustc0106/vllm-lt/blob/526a065721075a2f7809e26ba6b45ac3ca3c59cc/docs/benchmarks/q2-external-contract-20260911.md), [corrected attempt](https://github.com/hsliuustc0106/vllm-lt/blob/526a065721075a2f7809e26ba6b45ac3ca3c59cc/docs/benchmarks/q2-external-contract-uuid-20260911.md); implementation design: [cached external design](https://github.com/hsliuustc0106/vllm-lt/blob/526a065721075a2f7809e26ba6b45ac3ca3c59cc/docs/benchmarks/q2-external-design.md).

## Acceptance / 验收标准

| Requirement | Disposition |
| --- | --- |
| AC-Q2-04: genuine cached external generation and all64 IDs | Passed for the declared FP32 W1 scope. |
| AC-Q2-05: complete controlled timing comparison | Unaccepted/inconclusive: all four raw timing rows exist, but CPU preparation overlapped N2. |
| AC-Q2-06: auditable bounded external report | Preserve both attempts, automated report and manual limitation; verified evidence publication is required to complete the handoff; see PR17 for its link and checksums. |
| AC-Q2-01–03: task-quality/paired qualification | Separate PR18; BF16/adaptive prerequisites remain blocked by Q1. |

The one-off comparison harness has been removed from the active source tree. This historical run does not complete Q2’s timing milestone. Keep the prior accepted synchronous runtime and do not infer adaptive quality, broad serving performance or statistical confidence from these observations.
