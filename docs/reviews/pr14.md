# PR14 review follow-up

Fixed the duplicated metadata byte literal: preallocation accounting and actual metadata allocation now share one shape/dtype specification. The existing payload snapshot test additionally checks the derived count. The supported capacity, counted compact fallback for larger scheduler batches, broad terminal failure/interrupt semantics and quarantine's worker-terminal recovery boundary are now explicit in `docs/m3-persistent-buffers.md`.

Validation: 28 persistent-buffer/KV CPU tests passed with CUDA blocked; Ruff passed. Allocated shapes, dtypes, device bytes, traversal math and failure behavior remain unchanged; only cold setup accounting and documentation change. Original allocating-versus-persistent A/B evidence remains historical and makes no compact-path speed claim.

The requested future graph tests must preserve independent coda-held publication, generation/allocation checks, cancellation/slot reuse, and completion before LAST-EXITED reclamation. No copy elimination or asynchronous recovery protocol is introduced by this fix.
