# PR18 review follow-up — 2026-09-11

The original frozen FP32 quality observation remains **4/64 correct**, with 48 missing and 10 malformed final-answer markers. This is a protocol-specific baseline, not a localized engine defect or a general estimate of checkpoint accuracy. No generation, rescoring, parser change or budget extension was performed in this review pass.

## Preserved-output and tokenizer audit

The [CPU audit](tools/audit_q2_quality_format.py) verifies all 64 evaluation result hashes against the original primary report, inventories mechanical text markers and checks that source bytes remain unchanged. Its [machine-readable result](evidence/q2-quality-format-audit.json) records 57 length-limit and seven EOS terminations. Overlapping lexical flags count 17 outputs containing four hashes, 11 containing a shorter hash heading, 11 containing a Question heading, 40 containing a chat-start token and 42 containing a chat-end token. These flags are neither mutually exclusive nor correctness judgments; their presence alone does not explain the low score.

The pinned checkpoint metadata specifies GPT2Tokenizer, BOS/EOS `<|endoftext|>` ID0, and no tokenizer.json post-processor. Chat-start and chat-end are IDs1 and2; chat-end ID2 is not the configured EOS0. A chat template is present. The pinned README Quick Start directly tokenizes a raw completion prompt (`The future of AI is`), so the original raw-completion interface is consistent with that example. This metadata inspection does not establish that the chosen benchmark prompt is optimal, nor execute the tokenizer. Exact inspected-file hashes are retained in the audit.

## Remaining diagnostic work

A separately frozen, bounded native/official comparison should generate actual longer histories with the same prompt tokens, FP32 policy, four loops and output budget, recording the first token/logit divergence and EOS behavior. Checkpoint-template and longer-budget experiments must be labeled separate diagnostics with independently declared controls and limits. They must preserve the original score and cannot retroactively qualify it. These GPU experiments remain unexecuted; BF16/adaptive and full Q2 acceptance remain open.

## Validation

The 128 targeted data, schema, scorer and controller tests passed under the CUDA-blocked CPU runner (46.83 seconds). Ruff and formatting passed. The synthetic schema fixture now explicitly uses historical production blobs for its frozen source contract after parent review fixes; the production validator, frozen plan and original evidence remain unchanged. The format inventory adds zero GPU runs and does not support a new performance claim.
